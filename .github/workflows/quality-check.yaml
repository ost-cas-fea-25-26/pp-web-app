name: "🛡️ Quality Check"

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main
      - "gh-readonly-queue/**"

jobs:
  lint:
    name: "✨ Lint"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npm run lint

  test:
    name: "🧪 Unit Test + Coverage"
    runs-on: ubuntu-latest
    outputs:
      coverage: ${{ steps.coverage.outputs.path }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npm run test:unit:coverage

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v5
        with:
          name: coverage-report
          path: coverage
          if-no-files-found: error

  e2e:
    name: "📸 E2E Test — ${{ matrix.browser }} — shard ${{ matrix.shard }}"
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]
        shard: [1/2, 2/2]

    container:
      image: mcr.microsoft.com/playwright:v1.56.1-jammy

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: pp-user
          POSTGRES_PASSWORD: pp-password
          POSTGRES_DB: pp-database
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U pp-user"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      AUTH_CLIENT_ID: ${{ secrets.AUTH_CLIENT_ID }}
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
      API_URL: https://qwacker-api-http-prod-927407260649.europe-west6.run.app
      NEON_DATABASE_URL: postgresql://pp-user:pp-password@postgres:5432/pp-database
      VERCEL_URL: http://localhost:3000
      PLAYWRIGHT_TEST_USER_1_EMAIL: mr_playwright@test.com
      PLAYWRIGHT_TEST_USER_1_PASSWORD: ${{ secrets.PLAYWRIGHT_TEST_USER_1_PASSWORD }}
      HOME: /root
      CI: true

    steps:
      - uses: actions/checkout@v5

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright Tests (sharded)
        run: npx playwright test --project=${{ matrix.browser }} --shard=${{ matrix.shard }}
        env:
          DEBUG: pw:webserver

      - name: Upload Test Report (only if failure)
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report-${{ matrix.browser }}-${{ github.sha }}
          path: playwright-report/
